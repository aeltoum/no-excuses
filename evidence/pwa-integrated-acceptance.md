# PWA candidate acceptance record

Issue #158 continuation. Base commit: `1f78bf3`. Validated candidate: working tree on `codex/pwa-integrated-acceptance`, 2026-09-13. Active True MVP: iPhone/Android browser PWA. No hosted service or release deployment included.

## Verified automated scope

- `pnpm check`: pass (36 test files, 196 tests). Includes format, generated contracts, TypeScript, unit tests, secrets, and dependency policy.
- `pnpm web:test:browser`: baseline pass (42 tests; opt-in live tests skipped). Exact local URL: `http://127.0.0.1:4174/`. Playwright projects use `Desktop Chrome` and `Desktop Safari`; individual specs set compact and enlarged viewports where stated. Tests cover shell routes, manifest/icons, keyboard, 200% text, reduced motion, axe, OTP and Group UI, and weekly target/check-in/history flow.
- WebKit weekly axe regression: initial 200% run found transient `.lead` contrast below 3:1 while `.hero` faded in. Removed opacity tween; retained short transform reveal. Focused WebKit weekly test and full 42-test browser suite pass after repair.
- Browser specs assert page errors, console errors, and failed requests. Screenshots: `apps/web/test-results/**/verdict.png` and `apps/web/test-results/**/weekly-verdict.png` (local, ignored test artifacts). Expected simulated API error responses are part of failure-state tests.

Baseline browser tests route Supabase Auth and `/v1/**` API requests to fixtures. Separate `live-local.spec.ts` makes no fixture routes. At 390 × 844 in Chromium and WebKit, 4 opt-in tests passed against local Supabase Auth, loopback Node/psql API, and PostgreSQL schema version 14: real six-digit OTP; session restoration; Group create/invite/join; Weekly-target change; self-reported check-in; authoritative current count; empty finalized-history read. An opt-in first target request returned 503 before DB mutation; UI retained `3`, retry reused the idempotency key, and target change succeeded. Browser page errors and failed requests were empty; one expected 503 HTTP/console error was asserted and cleared before final no-error check. API checks using real Auth tokens returned generic 403 responses for cross-Group progress/history, ended membership, access cutoff, and deleted Account. After cutoff denial, test cleared cutoff and confirmed `current` returned 200 with null membership before marking Account deleted; subsequent 403 therefore exercises deleted status independently. No private Group facts appeared in denial responses. Captured verdict screenshots: [Chromium](pwa-local-browser/chromium/) and [WebKit](pwa-local-browser/webkit/), each covering Group, peer joined, Home before/after, Target failure/retry, and History. OTP, invitation token, and email are absent from selected screenshots.

`pnpm db:test:pwa-settlement` applied migrations to a disposable local PostgreSQL database, reused `scripts/fixtures/real-postgres-weekly-settlement.sql`, and dropped that database afterward (zero `no_excuses_pwa_%` databases remained). Checks passed: no finalization one second before boundary; exactly two active weeks finalized at boundary (Met/attained with 2 of 2, Missed with 1 of 2); replay finalized zero; terminal outcome unchanged; finalized-history count three, with pre-membership records excluded. Existing local database was not reset or modified by this settlement test. Live browser Auth/API checks created synthetic local users and Groups, which remain in the existing local database.

## Release gates still open

- Real iPhone Safari and Android Chrome: browser use, installation, standalone launch, session restoration after OTP and relaunch, keyboard/safe-area layout, 200% text, reduced motion, screen-reader and alternative-input paths. Use [device record](pwa-real-device-template.md).
- Confirm pilot support floor on actual devices: iOS/Safari 16.4+ and Chrome/Android 111+ per ADR 0005, or open compatibility ticket with observed OS/browser and failing behavior.
- Finalized Met/Missed browser presentation on a naturally elapsed live week, full negative authorization matrix, hosted service, and release deployment remain unverified. The local adapter covers only routes exercised by the active weekly loop and is not a production API server.

Status: **local browser integration passed for named flow; physical-device and remaining release gates open**. No hosting, provisioning, or spending performed.
